# 웹 화면 수정 및 Render 반영 가이드

## 개요
로컬에서 웹 화면을 수정한 후 Render에 자동 배포하는 절차를 설명합니다.

---

## 수정 → 배포 흐름도

```
┌─────────────────┐
│  1. 로컬 수정    │  코드/스타일/화면 수정
└────────┬────────┘
         ▼
┌─────────────────┐
│  2. 로컬 테스트  │  python run.py → localhost:5000 확인
└────────┬────────┘
         ▼
┌─────────────────┐
│  3. Git 스테이징 │  git add .
└────────┬────────┘
         ▼
┌─────────────────┐
│  4. Git 커밋    │  git commit -m "메시지"
└────────┬────────┘
         ▼
┌─────────────────┐
│  5. GitHub Push │  git push
└────────┬────────┘
         ▼
┌─────────────────┐
│  6. 자동 배포    │  Render가 자동 감지 → 빌드 → 배포
└────────┬────────┘
         ▼
┌─────────────────┐
│  7. 배포 완료    │  https://interest-rate-monitor.onrender.com
└─────────────────┘
```

---

## 상세 절차

### Step 1: 로컬에서 코드 수정

수정 가능한 주요 파일:

| 파일 | 설명 |
|------|------|
| `templates/index.html` | HTML 구조, 레이아웃 |
| `static/css/style.css` | 스타일, 색상, 폰트 |
| `static/js/app.js` | 메인 JavaScript 로직 |
| `static/js/charts.js` | 차트 설정 및 스타일 |
| `app/routes.py` | API 라우트 |
| `app/services/*.py` | 백엔드 서비스 로직 |

### Step 2: 로컬 테스트

```bash
# 프로젝트 폴더로 이동
cd interest-rate-monitor

# 가상환경 활성화 (Windows)
interestratemonitoring\Scripts\activate

# 서버 실행
python run.py
```

브라우저에서 http://127.0.0.1:5000 접속하여 수정사항 확인

### Step 3: Git 스테이징

```bash
# 모든 변경 파일 스테이징
git add .

# 또는 특정 파일만 스테이징
git add templates/index.html
git add static/css/style.css
```

### Step 4: Git 커밋

```bash
# 커밋 메시지와 함께 커밋
git commit -m "UI: 스프레드 차트 날짜 표시 추가"
```

**커밋 메시지 작성 팁**:
- `UI:` - 화면/디자인 변경
- `Fix:` - 버그 수정
- `Feature:` - 새 기능 추가
- `Style:` - CSS 스타일 변경
- `Refactor:` - 코드 리팩토링

### Step 5: GitHub에 Push

```bash
git push
```

> 첫 push 이후에는 `git push`만 입력하면 됩니다.

### Step 6: Render 자동 배포

Push 후 Render에서 자동으로:
1. GitHub 변경 감지 (약 1분 이내)
2. 빌드 시작 (`pip install -r requirements.txt`)
3. 서버 재시작 (`python run.py`)
4. 배포 완료

### Step 7: 배포 확인

- **배포 상태 확인**: [Render Dashboard](https://dashboard.render.com)
- **라이브 사이트**: https://interest-rate-monitor.onrender.com

---

## 한 줄 요약 명령어

```bash
git add . && git commit -m "변경 내용" && git push
```

---

## 배포 상태 확인 방법

### Render Dashboard에서 확인
1. https://dashboard.render.com 접속
2. `interest-rate-monitor` 서비스 클릭
3. `Events` 탭에서 배포 진행 상황 확인
4. `Logs` 탭에서 실시간 로그 확인

### 배포 상태 아이콘
| 상태 | 의미 |
|------|------|
| 🟢 Live | 정상 배포 완료 |
| 🟡 Building | 빌드 진행 중 |
| 🔴 Failed | 배포 실패 (로그 확인 필요) |

---

## 자주 하는 수정 예시

### 예시 1: 색상 변경

**파일**: `static/css/style.css`
```css
/* 분석 카드 배경색 변경 */
.analysis-card {
    background: linear-gradient(135deg, #1e3a5f 0%, #0d1b2a 100%);
}
```

### 예시 2: 텍스트 변경

**파일**: `templates/index.html`
```html
<!-- 제목 변경 -->
<h1 class="logo-text">Interest Rate Monitor</h1>
```

### 예시 3: 차트 설정 변경

**파일**: `static/js/charts.js`
```javascript
// X축 날짜 표시 형식 변경
ticks: {
    maxTicksLimit: 10,
    callback: function(value, index) {
        const date = new Date(this.getLabelForValue(value));
        return date.toLocaleDateString('ko-KR', {
            month: 'short',
            day: 'numeric'
        });
    }
}
```

---

## 문제 해결

### Push 후 변경사항이 반영되지 않을 때

1. **Render Dashboard 확인**
   - 배포가 진행 중인지 확인
   - 빌드 실패 여부 확인

2. **브라우저 캐시 삭제**
   - `Ctrl + Shift + R` (강력 새로고침)
   - 또는 시크릿 모드에서 확인

3. **Render 수동 배포**
   - Dashboard → `Manual Deploy` → `Deploy latest commit`

### 배포 실패 시

1. Render Dashboard → `Logs` 탭 확인
2. 에러 메시지 분석
3. 로컬에서 수정 후 다시 push

---

## 배포 소요 시간

| 단계 | 예상 시간 |
|------|----------|
| GitHub Push | 즉시 |
| Render 감지 | 약 1분 |
| 빌드 | 약 1-2분 |
| 배포 완료 | 약 30초 |
| **총 소요 시간** | **약 3-5분** |

---

*Last Updated: 2025-12-20*
